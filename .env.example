# --- Credenciales EMT MobilityLabs (https://mobilitylabs.emtmadrid.es) -------------------
# Opción A: usuario/contraseña del portal (cuota "genérica", más baja).
EMT_EMAIL=tu-email@example.com
EMT_PASSWORD=tu-password
# Opción B (recomendada): registra una App en el portal y usa su ClientId/passKey.
# Si se rellenan, tienen prioridad sobre email/password.
EMT_CLIENT_ID=
EMT_PASS_KEY=

# --- Qué recolectar --------------------------------------------------------------------
# Líneas a seguir (etiqueta pública o id interno: 27, 045, C1...). Se consultan todas
# sus paradas en ambos sentidos. Obligatorio EMT_LINES y/o EMT_STOPS.
EMT_LINES=27,45
# Paradas adicionales concretas (ids EMT). En estas paradas se guardan TODAS las líneas.
EMT_STOPS=
# Cada cuántas horas se refresca la lista de paradas de las líneas.
EMT_STOPS_REFRESH_HOURS=24

# --- Ritmo y cuota ----------------------------------------------------------------------
COLLECT_INTERVAL_SECONDS=60
# Límite cliente de peticiones/minuto (token bucket).
EMT_MAX_REQUESTS_PER_MINUTE=100
# Presupuesto diario (hits/día) para avisar si la configuración lo excede.
EMT_DAILY_REQUEST_BUDGET=150000
EMT_REQUEST_TIMEOUT_SECONDS=15
EMT_MAX_RETRIES=4
EMT_BASE_URL=https://openapi.emtmadrid.es

# --- Base de datos ----------------------------------------------------------------------
# Credenciales de Postgres. POSTGRES_PASSWORD es obligatoria (docker-compose no arranca sin
# ella): genera una con `openssl rand -hex 24`; puede contener cualquier carácter.
# El collector construye la URL de conexión con estas variables (host: "db" en compose,
# POSTGRES_HOST=localhost por defecto fuera de él).
POSTGRES_USER=emt
POSTGRES_PASSWORD=
POSTGRES_DB=emt
# Alternativa: URL SQLAlchemy completa; tiene prioridad sobre POSTGRES_* (útil para
# volúmenes creados con otra contraseña, otra BD o sqlite en pruebas).
# DATABASE_URL=postgresql+psycopg://emt:<password>@localhost:5432/emt
# Intentar crear hypertables TimescaleDB (se degrada a Postgres normal si no está).
DB_USE_TIMESCALE=true
# Políticas Timescale que init-db/run aplican a bus_positions y arrival_estimates (0 = quitar):
# comprimir chunks con más de N días (ahorra ~10x de disco; las consultas siguen funcionando)
# y BORRAR datos con más de N días (desactivado por defecto: el histórico es el objetivo).
DB_COMPRESS_AFTER_DAYS=7
DB_RETENTION_DAYS=0

# --- Bot de Telegram (emt-bot, opcional) -------------------------------------------------
# Token de @BotFather. Sin él, emt-bot no arranca (el resto del sistema no lo necesita).
TELEGRAM_BOT_TOKEN=
# Chats autorizados a consultar (vacío = cualquiera). Tu id: escribe a @userinfobot.
TELEGRAM_ALLOWED_CHAT_IDS=
# Chats que reciben alertas de bunching/saturación (vacío = sin alertas).
TELEGRAM_ALERT_CHAT_IDS=
# Cada cuántos minutos se evalúan los modelos, probabilidad mínima para avisar y
# enfriamiento por ruta para no repetir la misma alerta.
TELEGRAM_ALERT_EVERY_MINUTES=5
TELEGRAM_ALERT_PROBABILITY=0.6
TELEGRAM_ALERT_COOLDOWN_MINUTES=30
# Carpeta de emt-analysis (latest.json) de donde el bot carga los modelos entrenados.
REPORTS_DIR=reports

# --- Logging ----------------------------------------------------------------------------
LOG_LEVEL=INFO
# json (producción) | console (desarrollo)
LOG_FORMAT=json
